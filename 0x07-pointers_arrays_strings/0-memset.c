#include "main.h"
/**
 *_memset - fills memory
 *@s: string to be encoded
 *@b: value
 *@n: bytes
 *Return: detination
 */
 char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));
}
