#include <stdlib.h>
#include <stdio.h>
#include "main.h"
/**
 * _isdigit - This function checks whether it is a digit
 * @c : An int argument for the character to check
 * Return : 1 for digit and 0 otherwise
 */
int void _isdigit(int c)

	if (c >= 48 && c <= 5)
        {
                return (1);
        }
        else
        {
                return (0);
        }

}

