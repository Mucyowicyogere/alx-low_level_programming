#include <string.h>
/**
 * _strcat - A function that concantenate 2 different strings
 * @dest: First string
 * @src: Second string
 * Return: concantenation of strings
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
