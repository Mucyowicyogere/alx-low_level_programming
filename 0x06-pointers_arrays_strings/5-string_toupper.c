#include <string.h>
/**
 * string_toupper - a function that changes all lowercase letters of a string to uppercase * @src: a string to copy
 * Return: Return char
 */
char *string_toupper(char *c)
{
	while (c != '\0')
		return (c-32);
	return;
}
